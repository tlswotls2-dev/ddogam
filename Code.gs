// ==========================================
// [한글 주석] Google Apps Script (Code.gs) 수정본
// ==========================================

/**
 * [수정 사항: checkBattleMatch 함수에서 날짜 파싱 로직 수정]
 * 
 * 한국어 날짜 형식 파싱 불가 문제 해결을 위해
 * 대기 시작 시간(new Date(rows[i][3]))의 차이를 비교하는 대신,
 * 1분(60초) 대기는 클라이언트(battle.js)에서 보장하므로
 * 무조건 후보(candidates)에 추가하도록 변경합니다.
 */

// -------------------------------------------------------------
// [수정 전 기존 코드]
// -------------------------------------------------------------
/*
const waitStart = new Date(rows[i][3]);
const waitSeconds = (now - waitStart) / 1000;
if (waitSeconds >= 60) {
  candidates.push({ row: i + 1, number: rows[i][1] });
}
*/

// -------------------------------------------------------------
// [수정 후 변경 코드]
// -------------------------------------------------------------
// [한글 주석] 한국어 날짜 형식 파싱 불가 문제 해결
// 대기 시작 시간 대신 무조건 후보에 추가 (1분은 클라이언트에서 보장)
candidates.push({ row: i + 1, number: rows[i][1] });


// ==========================================
// 💡 안내 (저장 및 재배포 방법)
// ==========================================
/*
 현재 로컬 PC 환경에는 Google Apps Script API 커맨드라인 툴(clasp) 및 
 로그인 세션 자격 증명(.clasp.json 등)이 설정되어 있지 않습니다.
 따라서 로컬 컴퓨터에서 서버로 자동 재배포 명령을 실행할 수 없습니다.

 대신 아래 순서에 따라 Google Apps Script 웹 에디터에서 수동으로 수정 및 배포를 완료해 주세요!

 1. 브라우저를 열고 다음 URL 또는 스프레드시트의 확장 프로그램 -> Apps Script로 접속합니다.
    (스프레드시트에 연결된 스크립트 에디터로 이동)
 2. 스크립트 에디터의 'Code.gs' 파일 내에서 `checkBattleMatch` 함수를 찾습니다.
 3. 위 [수정 전 기존 코드] 영역을 [수정 후 변경 코드] 영역으로 교체하여 적용합니다.
 4. 상단의 '저장' (Ctrl + S 또는 디스크 모양 아이콘) 버튼을 클릭합니다.
 5. 우측 상단의 '배포' -> '배포 관리' 버튼을 클릭합니다.
 6. 현재 사용 중인 배포 버전(보통 웹 앱 형태)을 편집(연필 모양)하여, 
    버전을 '새 버전'으로 선택하고 '배포' 버튼을 눌러 재배포를 완료합니다!
*/
